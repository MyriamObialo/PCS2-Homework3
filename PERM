from itertools import permutations
import itertools

def perm (x):
    l= []
    p= permutations(range(1, x+1))
    for a in range(1,x+1):
        l.append(a)
    list2 = list(itertools.permutations(l, x))
    print(len(list2))
    for x in p:
        print(*x)
